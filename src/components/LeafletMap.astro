---
import "leaflet/dist/leaflet.css";
---

<div id="map"></div>

<script>
  import { LatLngExpression, Map, Marker, TileLayer } from "leaflet";
  import L from "leaflet";

  const center: LatLngExpression = [
    47.04679167910088, 15.451506793222032,
  ] as LatLngExpression;
  const map: Map = L.map("map").setView(center, 17);

  L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
    attribution:
      '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
  }).addTo(map) as TileLayer;

  L.marker(center)
    .addTo(map)
    .bindPopup("Top 9, Anton-Lippe-Platz 3, A-8041 Graz")
    .openPopup() as Marker;
</script>

<style>
  #map {
    height: 500px;
  }

  @media only print and (max-width: 1000px) {
    #map {
      height: 200px;
    }
  }
</style>
